name: Generate profile art

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build glitch avatar
        run: python3 scripts/glitch_avatar.py --user "${{ github.repository_owner }}" --out dist/glitch-avatar.svg

      - name: Build contribution snake
        uses: Platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-snake.svg?color_snake=#ff2a6d&color_dots=#ebedf0,#c7f5fa,#05d9e8,#b14aed,#ff2a6d
            dist/github-snake-dark.svg?palette=github-dark&color_snake=#ff2a6d&color_dots=#161b22,#2b0a4a,#7700ff,#d300c5,#05d9e8

      - name: Push to the output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
